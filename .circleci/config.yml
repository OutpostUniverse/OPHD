version: 2.0
jobs:
  build:
    docker:
      - image: outpostuniverse/nas2d-circleci:1.1
    steps:
      - checkout
      - run: git submodule update --init
      - run: make --keep-going nas2d
      - run: make --keep-going
